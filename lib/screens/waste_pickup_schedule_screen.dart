import 'package:flutter/material.dart';

class WastePickupScheduleScreen extends StatelessWidget {
  const WastePickupScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Waste Pickup Schedule'),
      ),
      body: const Center(
        child: Text('Coming soon: Garbage calendar & truck map'),
      ),
    );
  }
}